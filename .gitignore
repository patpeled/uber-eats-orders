# OS
.DS_Store
Thumbs.db
desktop.ini

# Claude Code runtime files
.claude/sessions/
.claude/backups/
.claude/shell-snapshots/
.claude/telemetry/
.claude/session-env/
.claude/settings.local.json

# Sensitive / local-only
*.env
*.local

# Uber Eats skill — per-user config (contains personal Slack channel, payment method, address)
# Note: config.json lives at the skill root by convention; assets/config.example.json is the committed template.
.claude/skills/uber-eats-order/config.json
